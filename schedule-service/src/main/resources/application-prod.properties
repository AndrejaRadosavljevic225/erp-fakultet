# PROD profil: Hibernate SAMO validira semu (ne dira je), bez SQL logova.
# JWT_SECRET i DB_* se OBAVEZNO postavljaju preko env varijabli.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
